# Image assets

Drop the two real photos here with these exact names:

| File | Used in | Recommended |
|---|---|---|
| `team.jpg` | Hero background (top of page) | Landscape, min 1920px wide, JPG ~300-500KB |
| `owners.jpg` | About Us section (Micah Garner & Doug Strobel) | Portrait, min 1200px wide |

Both are optional at runtime: if `team.jpg` is missing the hero falls back to the
blueprint-grid/radar background, and if `owners.jpg` is missing a clearly marked
placeholder is shown. No other changes are needed — just add the files.

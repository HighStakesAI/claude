# High Stakes — working notes for Claude

## Business contact details (use these everywhere)

- **Email:** ben@histakesai.com — use this for all contact info, legal pages, forms, and accounts. Do not use any other address.
- **Phone:** (850) 943-2040 (`tel:8509432040`, schema `+18509432040`)
- **Location:** Pensacola, FL

## Site

- `production-site/` is the live marketing site (static HTML/CSS/JS, no build step), deployed by the `highstakesai` Cloudflare Worker on push to `main`.
- Brand: HSA Brand Guidelines v1.1 — Stakes Gold `#C9A84C` on Obsidian `#0E0F12`, Caladea Bold display + Poppins body.
- Meta Pixel `1971813787556582` is installed on every page. Do not change pixel or conversion events without the owner's explicit approval.
- Privacy policy lives at `/privacy-policy` (`/privacy` is an alias).
